pipeline {
    agent {
        label 'electronix'
    }

    stages {
        stage("Tools configure") {
            steps {
                echo "Tools installing pipeline"
            }
        }

        stage("List Update") {
            steps {
                sh '''
                sudo apt-get update -y
                '''
            }
        }
    }
}
